export interface BasicType {
  loading: boolean,
  error: boolean,
  count: number,
  next: string,
  previous: string
}

export interface BasicStateDetails {
  loading: boolean,
  error: boolean,
}

export interface BasicTypeObject {
  created: string,
  edited: string,
  url: string
}
