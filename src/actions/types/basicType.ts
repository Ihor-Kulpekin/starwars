export interface BasicType {
  loading: boolean,
  error: boolean,
  count: number,
  next: string,
  previous: string
}


export interface BasicTypeObject {
  created: string,
  edited: string,
  url: string
}
